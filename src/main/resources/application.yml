server:
  port: 8080
  servlet:
    context-path: /api
spring:
  application:
    name: apiservice
authURL: ${AUTH_URL:test1234}
ansparURL: ${ANSPAR_URL:test1234}
certURL: ${CERT_URL:test1234}
management:
  health:
    probes:
      enabled: true